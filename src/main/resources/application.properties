# Configuration BD
spring.datasource.url=jdbc:mysql://localhost:3306/bestreader_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Lyne002Assouh
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuration JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Configuration Thymeleaf
spring.thymeleaf.cache=false

# Configuration Serveur
server.port=8080

# Nom de l'appli
spring.application.name=Best_Reader

# Upload de fichiers (PDF, Word)
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# JWT
jwt.secret=BestReaderSecretKey2024@#$%^&*SuperSecureKey
jwt.expiration=86400000